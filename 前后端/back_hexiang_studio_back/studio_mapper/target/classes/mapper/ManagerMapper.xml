<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.back_hexiang_studio.mapper.ManagerMapper">

    <!-- 添加管理员 -->
    <insert id="addWithManager" parameterType="com.back_hexiang_studio.dv.dto.UserDto" useGeneratedKeys="true" keyProperty="user_id">
        INSERT INTO manager (user_id)
        VALUES (#{user_id})
    </insert>

    <!-- 修改管理员信息 -->
    <update id="updateWithManager" parameterType="com.back_hexiang_studio.dv.dto.UserDto">
        UPDATE manager
        SET update_time = #{updateTime}
        WHERE user_id = #{user_id}
    </update>

    <!-- 删除管理员记录 -->
    <delete id="deleteManager" parameterType="java.lang.Long">
        DELETE FROM manager
        WHERE user_id = #{userId}
    </delete>

    <delete id="deleteManagerByUserIds" parameterType="java.util.List">
        DELETE FROM manager
        WHERE user_id IN
        <foreach item="userId" collection="userIds" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

</mapper>

